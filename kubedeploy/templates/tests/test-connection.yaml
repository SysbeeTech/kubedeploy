apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubedeploy.fullname" . }}-test-connection"
  labels:
    {{- include "kubedeploy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubedeploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
